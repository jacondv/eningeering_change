<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_qc_checksheet_inventor_import_wizard_form" model="ir.ui.view">
        <field name="name">qc.checksheet.inventor.import.wizard.form</field>
        <field name="model">qc.checksheet.inventor.import.wizard</field>
        <field name="arch" type="xml">
            <form string="Import from Inventor BOM">
                <field name="checksheet_id" invisible="1"/>
                <group>
                    <field name="bom_id"/>
                </group>
                <field name="line_ids">
                    <list editable="bottom">
                        <field name="selected" widget="boolean_toggle"/>
                        <field name="item"/>
                        <field name="part_number"/>
                        <field name="description"/>
                        <field name="qty"/>
                        <field name="image" widget="image" options="{'size': [64, 64]}"/>
                    </list>
                </field>
                <footer>
                    <button string="Import" name="action_import" type="object" class="btn-primary"/>
                    <button string="Cancel" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_qc_checksheet_inventor_import_wizard" model="ir.actions.act_window">
        <field name="name">Import from Inventor BOM</field>
        <field name="res_model">qc.checksheet.inventor.import.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
