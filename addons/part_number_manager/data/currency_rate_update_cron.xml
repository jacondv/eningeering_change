<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <!-- 8:00 AM GMT+7 = 01:00 UTC - nextcall is always stored/interpreted
         in UTC (confirmed the container's own clock is UTC), same pattern
         as engineering_change's own ir_cron_check_overdue_actions. -->
    <record id="ir_cron_currency_rate_update" model="ir.cron">
        <field name="name">Part Number Manager: Update Currency Rates</field>
        <field name="model_id" ref="base.model_res_currency"/>
        <field name="state">code</field>
        <field name="code">model._cron_update_rates()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 01:00:00')"/>
        <field name="active" eval="True"/>
    </record>

</odoo>
