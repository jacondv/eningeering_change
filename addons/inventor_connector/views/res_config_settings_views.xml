<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_inventor_connector_settings_form" model="ir.ui.view">
        <field name="name">inventor.connector.settings.form</field>
        <field name="model">res.config.settings</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <form string="Inventor Connector Settings">
                <sheet>
                    <group string="API Key">
                        <field name="inventor_connector_api_key" widget="CopyClipboardChar"
                               help="Shared secret Inventor scripts must send in the X-API-Key header when posting BOM data to /api/inventor/bom. Type your own value or use Regenerate to create a random one."/>
                        <button name="action_regenerate_inventor_api_key" type="object"
                                string="Regenerate" class="btn-secondary"/>
                    </group>
                    <group string="Payload Limit">
                        <field name="inventor_connector_max_payload_mb"
                               help="Reject incoming BOM requests larger than this size."/>
                    </group>
                </sheet>
                <footer>
                    <button string="Save" type="object" name="execute" class="btn-primary"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_inventor_connector_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_inventor_connector_settings_form"/>
        <field name="target">new</field>
    </record>

</odoo>
