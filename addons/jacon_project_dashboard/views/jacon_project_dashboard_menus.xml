<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Top-level item under the Project app's own menu bar, sibling to
         Projects/Tasks/Reporting/Configuration - one click away rather than
         nested inside Reporting, per explicit user preference. Visible to
         every internal user (not just Project Administrator) - the
         dashboard itself carries no per-record access restriction, it's a
         read-only aggregate view any employee should be able to check. -->
    <menuitem id="menu_jacon_project_dashboard"
              name="Hours Dashboard"
              parent="project.menu_main_pm"
              action="action_jacon_project_dashboard"
              sequence="99"/>

</odoo>
